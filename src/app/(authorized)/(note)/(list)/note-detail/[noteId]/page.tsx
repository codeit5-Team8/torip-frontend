'use client';

import NoteDetail from '@ui/note/NoteDetail';
// import { useParams } from 'next/navigation';

export default function NoteDetailPage() {
  // TODO noteId 받아서 api 호출 후 렌더링
  // const { noteId } = useParams();

  // TODO 페이지 직접 접근 시 UI 처리
  return <NoteDetail />;
}
